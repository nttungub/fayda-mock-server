full_profile.html references /css/bootstrap.min.css.
Either drop the real bootstrap.min.css file here, or change that template
to use the Bootstrap CDN. All other templates use inline CSS + the
Font Awesome CDN, so they need nothing here.
